{
  "name": "temu-lp-autofeed",
  "private": true,
  "scripts": {
    "parse:manual": "ts-node scripts/parse_manual.ts",
    "playwright:install": "npx playwright install --with-deps chromium"
  },
  "dependencies": {
    "csv-parse": "^5.5.6",
    "playwright": "^1.46.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  }
}
